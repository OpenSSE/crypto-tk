before_script:
  - apt-get update -qq && apt-get install -y -qq libssl-dev scons yasm libgmp-dev
  - ./install_relic.sh
 
stages:

  - build
  - test
  
crypto:
  stage: build
  script:
  - scons
  
check:
  stage: test
  script:
  - scons check  
  - scons check no_aesni=1
