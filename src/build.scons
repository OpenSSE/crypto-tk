Import('*')

files = Glob('*.cpp') + Glob('*/*.cpp') + Glob('*/*/*.cpp') + Glob('*/*/*/*.cpp')
files += Glob('hash/*/*.asm') + Glob('hash/*/*.c')
objs = env.Object(files, CPPPATH = ['.'])

Return('objs')